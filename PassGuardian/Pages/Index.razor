@page "/home"
@inject IToastService toast
@inject User user;
@inject Notification notification




@code {
    protected override async Task OnInitializedAsync()
    {
        if(notification.Welcome != true)
        {
            toast.ShowInfo("PassGuardian",$"Welcome {user.UserName}");
            notification.Welcome = true;
        }
    }
}